const sql = require('mssql')

var config = {
        user: 'sa',
        password: 'Cu@ng123',
        server: 'localhost', 
        database: 'blockchain',
        port: 1433
    }
module.exports = config