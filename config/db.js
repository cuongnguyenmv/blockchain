const sql = require('mssql')

const config = {
        user: 'sa',
        password: 'Cu@ng123',
        server: 'localhost', 
        database: 'blockchain',
        port: 1433
    }
module.exports = config